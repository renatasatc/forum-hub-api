package challenge_forum_hub.api.model;

/**
 * Representa o estado do tópico dentro do sistema
 * Pode ser usado para controlar se o tópico está visível/ativo ou não
 */
public enum EstadoTopico {

    // Tópico ativo e disponível
    ATIVO,

    // Tópico inativo (oculto ou desativado)
    INATIVO
}
